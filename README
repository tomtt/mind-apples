== Mind Apples

Rails application using Ruby 1.8.7 & Rails 2.3.4

Postgres database
  The application uses a postgres database. Bundle install will not run without postgres installed on your machine
  > brew install postgres
  
Using bundler for gem management

Deployment on heroku